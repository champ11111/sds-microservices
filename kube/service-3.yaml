apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-3
  labels:
    app: service-3
spec:
  selector:
    matchLabels:
      app: service-3
  replicas: 1
  template:
    metadata:
      labels:
        app: service-3
    spec:
      containers:
        - name: service-3
          image: champkub6/service3:v4.0.0
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 3

      nodeSelector:
        node-role.kubernetes.io/worker: worker
---
apiVersion: v1
kind: Service
metadata:
  name: son
spec:
  selector:
    app: service-3
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
